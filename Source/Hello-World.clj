;; Clojure is a dynamic and functional dialect of the Lisp programming language on the Java platform

;; This code prints `Hello, World!` by calling the print function which logs it out to the console
(print "Hello, World!")
;; In this programming language, the programmer has to be explicit managing identity and its states
;; With focus on working with Immutable values. The code in file is treated as data.